CREATE TABLE IF NOT EXISTS comment (
    number IDENTITY,
    user CHAR NOT NULL,
    userComment CHAR NOT NULL
);

CREATE TABLE IF NOT EXISTS comment2 (
    number IDENTITY,
    user CHAR NOT NULL,
    userComment CHAR NOT NULL
);
